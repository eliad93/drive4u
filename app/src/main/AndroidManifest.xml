<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eliad.drive4u">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.StudentScheduleLessonActivity" />
        <activity android:name=".activities.UserTypeChoiceActivity" />
        <activity android:name=".activities.TeacherHomeActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.TeacherScheduleActivity" />
        <activity android:name=".activities.StudentRegistrationActivity" />
        <activity android:name=".activities.StudentHomeActivity" />
        <activity android:name=".activities.StudentSearchTeacherActivity" />
        <activity android:name=".activities.TeacherRegistrationActivity" />
        <activity android:name=".activities.PresentLessonActivity" />
        <activity android:name=".activities.StudentPluralInfoActivity" />
        <activity android:name=".activities.TeacherProfileActivity" />
        <activity android:name=".activities.StudentProfileActivity" />
        <activity android:name=".activities.TeacherMyStudentsActivity" />
        <activity android:name=".activities.StudentPastLessonsActivity" />
        <activity android:name=".activities.TeacherStudentInfoActivity"></activity>
    </application>

</manifest>